# --- Core Framework & APIs ---
Django==5.2.17
djangorestframework==3.18.0
PyJWT==2.13.0
asgiref==3.12.1
sqlparse==0.4.3
tzdata>=2022.7

# --- Database Management ---
psycopg2-binary==2.9.9
dj-database-url==2.1.0
pgvector==0.5.0

# --- Environment & Configuration ---
python-dotenv==1.2.2
python-decouple==3.8

# --- Internal Microservice HTTP Client ---
# Already present transitively (google-genai, huggingface_hub) but pinned
# directly here since apps/catalog/signals.py now imports it explicitly.
httpx==0.28.1

# --- Data Processing & Export ---
pandas==2.2.0
openpyxl==3.1.2

# --- Media & File Handling ---
Pillow==10.4.0

# --- Persistent Media Storage (Cloudinary, production only) ---
# Render's web dyno disk is ephemeral -- product/profile images stored on it
# are lost on every redeploy. config/settings/production.py switches
# STORAGES['default'] to cloudinary_storage.storage.MediaCloudinaryStorage.
django-cloudinary-storage==0.3.0
cloudinary==1.45.0

# --- Testing & Mock Data ---
Faker==22.5.1
datasets==2.21.0

# --- Production & Deployment ---
gunicorn==21.2.0
whitenoise==6.6.0
